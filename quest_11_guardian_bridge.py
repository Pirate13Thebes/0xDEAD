# Level 3 begins: The Bridge Guardian uses 'if' to make a decision.
# The code inside the indented block only runs if the condition is True.
user_age = int(input("Halt! How old are you? "))

if user_age >= 18:
    print("You are 18 or older. You are eligible to vote and may pass the bridge!")